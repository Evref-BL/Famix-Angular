SmalltalkCISpec {
  #loading : [
    SCIMetacelloLoadSpec {
      #baseline : 'FamixAngular',
      #directory : 'src',
      #onConflict : #useIncoming,
      #onUpgrade : #useIncoming,
      #ignoreImage : true
    }
  ],
  #testing : {
    #packages : [
      'Famix-Angular-Tests'
    ]
  }
}
